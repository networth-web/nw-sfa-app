<%= form_with model: lead_seminar, url: update_approval_lead_seminar_path(lead_seminar) do |f| %>

  <!-- ヘッダー -->
  <div class="modal-header">
    <h5>不承認理由</h5>
    <button type="button" class="btn-close" data-mdb-dismiss="modal" aria-label="Close"></button>
  </div>
  <!-- ボディー -->
  <div class="modal-body pt-3 pb-5">
    <%= render 'layouts/error_messages', model: f.object %>

    <%= f.hidden_field :approval_id, :value => 2 %>

    <div>
      <%= f.label :disapproval_reason, '不承認理由' %>
      <%= f.text_area :disapproval_reason, class: "form-control mh-100p" %>
    </div>

  </div>
  <!-- フッター -->
  <div class="modal-footer">
    <button type="button" class="cancel-form btn btn-light" data-mdb-dismiss="modal" data-mdb-ripple-color="dark">閉じる</button>
    <%= f.submit "変更を保存", class: "btn btn-primary" %>
  </div>

<% end %>