<main class="pt-0">
  <!-- ナビゲーター -->
  <div class="position-absolute pt-3">
    <%= link_to sales_process_path(@sales_process), class: "" do %>
      <i class="fa-solid fa-chevron-left"></i> 戻る
    <% end %>
  </div>
  <!-- ヘッダー -->
  <h4 class="text-center py-4">営業プロセスを編集</h4>
  <!-- フォーム -->
  <%= render partial: 'form', locals: { sales_process: @sales_process } %>
</main>